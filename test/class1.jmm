class Fac {
  public int ComputeFac(int num) {
    int num_aux ;
    while (num < 5 && num < 2) {
      num = num - 1;
    }
    if (num < 1)
      num_aux = 1;
    else
      num_aux = num * (this.ComputeFac(num-1));
    return num_aux;
  }
  public static void main(String[] a){ //THIS IS A COMMENT
    io.println(new Fac().ComputeFac(10)); //IGNORE ME
  }
}
