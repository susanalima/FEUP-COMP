class HelloWorld {
	public boolean build_test_arr(int s, int b) {
		ioPlus.printHelloWorld();

		return false;
	}

	public boolean build_test_arr(boolean s) {
		return s;
	}

	public static void main(String[] args) {
		int s;
		int a; 

		s = 5;
		a = 4;

		HelloWorld.build_test_arr(s);
		this.build_test_arr(s, a);
		this.build_test_arr(12, 15);
		this.build_test_arr(false);
		ioPlus.printHelloWorld();
	}
}