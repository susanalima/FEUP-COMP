class Fac {
  public int ComputeFac(int num) {
    int num_aux ;
    while asd(num er< 5error) error {
      num = num - 1;
    }
    if (num < 1)
      num_aux = 1;
    else
      num_aux = num * (this.ComputeFac(num-1));
    return num_aux;
  }
  public static void main(String[] a){
    io.println(new Fac().ComputeFac(10)); 
  }
}
